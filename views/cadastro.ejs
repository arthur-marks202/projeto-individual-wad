<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Cadastro - Checkin Room</title>
  <link rel="stylesheet" href="/CSS/cadastro/styles.css">
</head>
<body>
  <a href="/home" class="btn-home">
    <span>🏠</span>
    <span>Home</span>
  </a>

  <div class="cadastro-container">
    <!-- Seção da Imagem -->
    <div class="cadastro-image">
      <div class="cadastro-image-content">
        <h2>Junte-se ao Checkin Room</h2>
        <p>Crie sua conta e comece a reservar<br>salas de forma simples e eficiente</p>
      </div>
    </div>

    <!-- Seção do Formulário -->
    <div class="cadastro-form">
      <div class="cadastro-header">
        <h1>Crie sua Conta</h1>
        <p>Preencha os dados para se cadastrar</p>
      </div>

      <!-- Mensagens de Feedback -->
      <% if (mensagemErro) { %>
        <div class="message erro"><%= mensagemErro %></div>
      <% } %>

      <% if (mensagemSucesso) { %>
        <div class="message sucesso"><%= mensagemSucesso %></div>
      <% } %>

      <!-- Formulário de Cadastro -->
      <form action="/usuarios" method="POST">
        <div class="form-group">
          <label for="nome">Nome completo</label>
          <input
            type="text"
            name="nome"
            id="nome"
            placeholder="Digite seu nome completo"
            required
          >
        </div>

        <div class="form-group">
          <label for="email">Email institucional</label>
          <input
            type="email"
            name="email"
            id="email"
            placeholder="seuemail@sou.inteli.edu.br"
            required
          >
        </div>

        <div class="form-group">
          <label for="senha">Senha</label>
          <input
            type="password"
            name="senha"
            id="senha"
            placeholder="Digite uma senha segura"
            required
          >
        </div>

        <div class="form-group">
          <label>Tipo de usuário</label>
          <div class="tipo-usuario">
            <div class="tipo-usuario-option aluno">
              <label>
                <input type="radio" name="tipo_usuario" value="aluno" required>
                Aluno
              </label>
            </div>
            <div class="tipo-usuario-option professor">
              <label>
                <input type="radio" name="tipo_usuario" value="professor">
                Professor
              </label>
            </div>
            <div class="tipo-usuario-option admin">
              <label>
                <input type="radio" name="tipo_usuario" value="admin">
                Admin
              </label>
            </div>
          </div>
        </div>

        <button type="submit" class="btn-primary">Cadastrar</button>
      </form>

      <!-- Link para Voltar -->
      <div class="link-voltar">
        <p>Já tem conta? <a href="/">Faça login</a></p>
      </div>
    </div>
  </div>
</body>
</html>

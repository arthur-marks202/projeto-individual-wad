<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Checkin Room</title>
  <link rel="stylesheet" href="/CSS/Login/styles.css">
</head>
<body>
  <a href="/home" class="btn-home">
    <span>🏠</span>
    <span>Home</span>
  </a>

  <div class="login-container">
    <!-- Seção da Imagem -->
    <div class="login-image">
      <div class="login-image-content">
        <h2>Checkin Room</h2>
        <p>Sistema de reserva de salas<br>do Inteli - Instituto de Tecnologia e Liderança</p>
      </div>
    </div>

    <!-- Seção do Formulário -->
    <div class="login-form">
      <div class="login-header">
        <h1>Bem-vindo de volta!</h1>
        <p>Faça login para acessar o sistema</p>
      </div>

      <!-- Mensagens de Feedback -->
      <% if (mensagemErro) { %>
        <div class="message erro"><%= mensagemErro %></div>
      <% } %>

      <% if (mensagemSucesso) { %>
        <div class="message sucesso"><%= mensagemSucesso %></div>
      <% } %>

      <!-- Formulário de Login -->
      <form action="/login" method="POST">
        <div class="form-group">
          <label for="email">Email institucional</label>
          <input
            type="email"
            name="email"
            id="email"
            placeholder="seuemail@sou.inteli.edu.br"
            required
          >
        </div>

        <div class="form-group">
          <label for="senha">Senha</label>
          <input
            type="password"
            name="senha"
            id="senha"
            placeholder="Digite sua senha"
            required
          >
        </div>

        <button type="submit" class="btn-primary">Entrar</button>
      </form>

      <!-- Link para Cadastro -->
      <div class="link-cadastro">
        <p>Não tem uma conta? <a href="/cadastro">Cadastre-se aqui</a></p>
      </div>
    </div>
  </div>
</body>
</html>
